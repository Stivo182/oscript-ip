#Использовать asserts
#Использовать errors
#Использовать "../fixtures"
#Использовать "../../src"

#Область Тесты

&ПараметризованныйТест
&ИсточникЗначение("0.0.0.0/0")
&ИсточникЗначение("192.168.1.0/24")
&ИсточникЗначение("192.168.1.42/32")
&ИсточникЗначение("::/0")
&ИсточникЗначение("2001:db8::/32")
&ИсточникЗначение("2001:db8::1/128")
Процедура ТестДолжен_РазобратьКорректныйСтрогийПрефикс(Значение) Экспорт

	// Действие
	Префикс = Новый IPПрефикс(Значение);

	// Проверка
	Ожидаем.Что(Префикс.ВСтроку()).Равно(Значение);

КонецПроцедуры

&Тест
Процедура ТестДолжен_КанонизироватьТекстIPv6ВСтрокеПрефикса() Экспорт

	// Действие
	Префикс = Новый IPПрефикс("2001:0DB8:0:0::/32");

	// Проверка
	Ожидаем.Что(Префикс.ВСтроку()).Равно("2001:db8::/32");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СкопироватьПрефикс() Экспорт

	// Подготовка
	ИсходныйПрефикс = Новый IPПрефикс("2001:db8::/32");

	// Действие
	Копия = Новый IPПрефикс(ИсходныйПрефикс);

	// Проверка
	Ожидаем.Что(Копия.ВСтроку()).Равно("2001:db8::/32");
	Ожидаем.Что(Копия.БазовыйАдрес().ИмеетЗону()).ЭтоЛожь();

КонецПроцедуры

&Тест
Процедура ТестДолжен_СоздатьСтрогийПрефиксИзКаноническогоIPv4АдресаИДлины() Экспорт

	// Действие
	Префикс = Новый IPПрефикс(Новый IPАдрес("192.168.1.0"), 24);

	// Проверка
	Ожидаем.Что(Префикс.ВСтроку()).Равно("192.168.1.0/24");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СоздатьСтрогийПрефиксИзКаноническогоIPv6АдресаИДлины() Экспорт

	// Действие
	Префикс = Новый IPПрефикс(Новый IPАдрес("2001:db8::"), 32);

	// Проверка
	Ожидаем.Что(Префикс.ВСтроку()).Равно("2001:db8::/32");

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("192.168.1.0")
&ИсточникЗначение("/24")
&ИсточникЗначение("192.168.1.0/")
&ИсточникЗначение("192.168.1.0/24/1")
&ИсточникЗначение("192.168.1.0/01")
&ИсточникЗначение("192.168.1.0/+24")
&ИсточникЗначение("192.168.1.0/-1")
&ИсточникЗначение("192.168.1.0/ 24")
&ИсточникЗначение("192.168.1.0/24 ")
&ИсточникЗначение("192.168.1.0/24.0")
&ИсточникЗначение("fe80::%eth0/64")
Процедура ТестДолжен_ОтклонитьНекорректныйТекстовыйПрефикс(Значение) Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Значение);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыОшибокIP.НедопустимыйПрефикс());

КонецПроцедуры

&Тест
Процедура ТестДолжен_СохранитьПричинуОшибкиАдресаВОшибкеПрефикса() Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса("999.1.1.1/24");

	// Проверка
	КодОшибки = КодыОшибокIP.НедопустимыйПрефикс();
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодОшибки);
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка.Причина, КодыОшибокIP.НедопустимыйIPv4());
	Снимок = ДиагностикаОшибок.НайтиПервую(Ошибка, КодОшибки);
	Ожидаем.Что(Снимок.Данные().Компонент).Равно("Адрес");
	Ожидаем.Что(Снимок.Данные().Ожидание).Равно("IPv4 или IPv6");

КонецПроцедуры

&Тест
Процедура ТестДолжен_УказатьКомпонентОшибкиДлиныПрефикса() Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса("192.0.2.0/2x");

	// Проверка
	КодОшибки = КодыОшибокIP.НедопустимыйПрефикс();
	Снимок = ДиагностикаОшибок.НайтиПервую(Ошибка, КодОшибки);
	Ожидаем.Что(Снимок.Данные().Компонент).Равно("ДлинаПрефикса");
	Ожидаем.Что(Снимок.Данные().Ожидание).Равно("целое число без ведущих нулей");

КонецПроцедуры

&Тест
Процедура ТестДолжен_УказатьКомпонентЗоныВПрефиксе() Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса("fe80::%eth0/64");

	// Проверка
	КодОшибки = КодыОшибокIP.НедопустимыйПрефикс();
	Снимок = ДиагностикаОшибок.НайтиПервую(Ошибка, КодОшибки);
	Ожидаем.Что(Снимок.Данные().Компонент).Равно("Зона");

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("192.168.1.0/33")
&ИсточникЗначение("2001:db8::/129")
&ИсточникЗначение("192.168.1.0/999999999999999999999999999999999999")
Процедура ТестДолжен_ОтклонитьДлинуТекстовогоПрефиксаВнеДиапазона(Значение) Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Значение);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыОшибокIP.ДлинаПрефиксаВнеДиапазона());

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("192.168.1.42/24")
&ИсточникЗначение("2001:db8::1/32")
Процедура ТестДолжен_ОтклонитьУстановленныеБитыУзлаВТекстовомПрефиксе(Значение) Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Значение);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыОшибокIP.УстановленыБитыУзла());

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОтклонитьЗонуВСтрогомКонструктореПрефикса() Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Новый IPАдрес("fe80::%eth0"), 64);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыОшибокIP.НедопустимыйПрефикс());

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("192.168.1.42", 24)
&ИсточникЗначение("2001:db8::1", 32)
Процедура ТестДолжен_ОтклонитьУстановленныеБитыУзлаВСтрогомКонструкторе(Адрес, Длина) Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Новый IPАдрес(Адрес), Длина);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыОшибокIP.УстановленыБитыУзла());

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("192.168.1.0", 33)
&ИсточникЗначение("2001:db8::", 129)
Процедура ТестДолжен_ОтклонитьДлинуСтрогогоПрефиксаВнеДиапазона(Адрес, Длина) Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Новый IPАдрес(Адрес), Длина);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыСтандартныхОшибок.АргументВнеДиапазона());

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОтклонитьДробнуюДлинуСтрогогоПрефикса() Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Новый IPАдрес("192.168.1.0"), 24.5);

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(
		Ошибка,
		КодыСтандартныхОшибок.НедопустимоеЗначениеАргумента()
	);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОтклонитьОтсутствующуюДлинуДляАдреса() Экспорт

	// Действие
	Ошибка = ОшибкаПриСозданииПрефикса(Новый IPАдрес("192.168.1.0"));

	// Проверка
	ПомощникТестированияIP.ПроверитьКодОшибки(Ошибка, КодыСтандартныхОшибок.АргументНеЗаполнен());

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ОшибкаПриСозданииПрефикса(Значение, Длина = Неопределено)

	Попытка
		Если Длина = Неопределено Тогда
			Префикс = Новый IPПрефикс(Значение);
		Иначе
			Префикс = Новый IPПрефикс(Значение, Длина);
		КонецЕсли;
	Исключение
		Возврат ИнформацияОбОшибке();
	КонецПопытки;

	Возврат Неопределено;

КонецФункции

#КонецОбласти
